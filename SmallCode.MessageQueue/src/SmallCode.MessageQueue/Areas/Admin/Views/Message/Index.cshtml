@using SmallCode.MessageQueue.Model
@model SmallCode.Pager.PagedList<Message>
@using SmallCode.Pager
@{
    Layout = "~/Areas/Admin/Views/shared/_LayoutPart.cshtml";
}
<link href="~/styles/page.css" rel="stylesheet" />
<div class="gray-bg wrapper wrapper-content animated fadeInUp">
    <div class="row">
        <div class="col-sm-12">
            <div class="ibox">
                <div class="ibox-title">
                    <h5>所有消息</h5>
                </div>
                <div class="ibox-content">
                    <div class="row m-b-sm m-t-sm">
                        <div class="col-md-1">
                            <button type="button" id="loading-example-btn" class="btn btn-white btn-sm"><i class="fa fa-refresh"></i> 刷新</button>
                        </div>
                        <div class="col-md-11">
                            <div class="input-group">
                                <input type="text" placeholder="请输入消息" class="input-sm form-control"> <span class="input-group-btn">
                                    <button type="button" class="btn btn-sm btn-primary">搜索</button>
                                </span>
                            </div>
                        </div>
                    </div>
                    <div class="project-list">
                        <table class="table table-hover">
                            <tbody>
                                <tr>
                                    <th>ID</th>
                                    <th>主题</th>
                                    <th>名称</th>
                                    <th>创建时间</th>
                                    <th>操作</th>
                                </tr>
                                @foreach (var item in Model.PageListData)
                                {
                                    <tr id="@item.Id">
                                        <td>@item.Id</td>
                                        <td>@item.Topic.Key</td>
                                        <td>@item.Content</td>
                                        <td>@item.CreateDate</td>
                                        <td>
                                            <a href="#" del-url="/Admin/Message/Delete/@item.Id" class="dataDel" data-id="@item.Id">删除</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

